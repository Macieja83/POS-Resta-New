# Backend env vars for Vercel (Settings -> Environment Variables)
# Copy variable names and set values in Vercel Dashboard. Use Production + Preview.

# Database - Supabase pooler (same as apps/backend/.env)
# Port 6543 = transaction pooler (use for serverless/Vercel)
DATABASE_URL="postgresql://...@...pooler.supabase.com:6543/postgres"

# Port 5432 = direct pooler (for migrations)
DIRECT_DATABASE_URL="postgresql://...@...pooler.supabase.com:5432/postgres"

# Server
NODE_ENV=production

# JWT Secret - use same as local or generate a long random string
JWT_SECRET="your-super-secret-jwt-key-here"

# CORS - exact frontend URL(s), comma-separated, no spaces
CORS_ORIGINS="https://pos-system-frontend.vercel.app"

